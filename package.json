{
  "name": "test1",
  "version": "1.0.0",
  "description": "Production-grade Node.js HTTP server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
